@using Microsoft.AspNetCore.Components.Routing


<MudPaper Width="400px" Class="py-3" Elevation="0">
    <MudNavMenu>
        <MudText Typo="Typo.h6" Class="px-4">TEKO</MudText>
        <MudText Typo="Typo.body2" Class="px-4 mud-text-secondary">Учет персоанала</MudText>
        <MudDivider Class="my-2" />
        <MudNavLink Href="/">Сотрудники</MudNavLink>
        <MudNavLink Href="/timeoff">Отпуска</MudNavLink>
        <MudNavLink Href="/about">Обо мне</MudNavLink>
    </MudNavMenu>
</MudPaper>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
